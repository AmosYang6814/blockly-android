逻辑层负责的工作：
  1.向UI层提供全部的组件信息，并通过反射机制，将数据持久层的组件信息，转化成针对不同平台的组件信息
  2.获取UI中暂存的屏幕信息、连接信息、复用信息、并将其转化成持久层的状态和关系
  3、解析每一个链接和状态，针对每一个连接和状态，提供一个逻辑片段插入ID
  4、获取UI层中所有的组件信息，并将组件信息传递到数据持久层

逻辑层包含的模块组件
  1.反射器模块，将组件进行反射，然后向上层提供，反射的依据是简单组件的名字
  2.状态及链接转化模块  向UI层、逻辑层提供状态查询、状态存储等功能